# GitHub Actions only. Library dependencies are handled by Scala Steward, which understands the
# Mill build; Dependabot does not, and would churn against pins it cannot read.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
