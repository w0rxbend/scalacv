---
name: Bug report
about: Something behaves differently from what the docs or types promise
labels: bug
---

**What happened**

**What you expected**

**Minimal reproduction**
```scala
```

**Environment**
- scalacv version:
- JDK (`java -version`):
- Platform and bytedeco classifier (e.g. `linux-x86_64`):
- OS:

**If the JVM crashed**
Attach `hs_err_pid*.log`. A native crash produces no Java stack trace, so that file is
usually the only evidence available.
