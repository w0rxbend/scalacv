# Mill
out/
.mill-daemon/

# zio-test writes its JUnit-style XML report here, relative to the process working directory,
# with no Mill task owning the path — so it lands at the repo root on every `./mill zio.test`
# and shows up as an untracked directory.
target/

# Build servers / IDE tooling
.claude/
.bsp/
.bloop/
.metals/
.scala-build/
.idea/
*.iml
.vscode/

# JVM
*.class
hs_err_pid*.log
replay_pid*.log

# OS
.DS_Store
Thumbs.db

# Docusaurus site (website/): generated docs + Scaladoc + build output are produced by
# scripts/assemble-docs.sh and are not committed. website/.gitignore covers most of these;
# these lines guard against running tooling from the repo root.
website/node_modules/
website/build/
website/.docusaurus/
website/docs/
website/static/api/
